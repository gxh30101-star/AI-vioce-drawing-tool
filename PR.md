# PR 描述文档

本文档包含语音绘图工具所有功能的 PR 描述，每个 PR 只做一件事。

---

## PR #1: 基础语音识别与唤醒词系统

### 标题
`feat: 添加语音识别和唤醒词"你好"系统`

### 功能描述
实现基于 Web Speech API 的语音识别功能，支持唤醒词"你好"激活工具。唤醒后 15 秒无操作自动进入待机状态。

### 实现思路
- 使用 `SpeechRecognition` API 实现中文语音识别
- 实现 `SpeechManager` 类管理语音生命周期
- 唤醒词检测：识别到"你好"后进入唤醒状态
- 待机定时器：15 秒无操作自动回到待机状态
- 回声消除：TTS 播报期间屏蔽语音识别

### 测试方式
1. 打开页面，显示"说'你好'唤醒我"
2. 说"你好"，界面变为唤醒状态
3. 等待 15 秒，自动回到待机状态

---

## PR #2: AI 图像生成集成

### 标题
`feat: 接入 agnes.ai API 实现 AI 图像生成功能`

### 功能描述
接入 agnes.ai 图像生成 API，支持用户通过语音描述生成任意图片。生成时间约 30-60 秒。

### 实现思路
- 服务器端：添加 `/api/generate-image` 端点，代理 agnes.ai API
- 客户端：实现 `ImageGenerator` 类，处理 API 请求和响应
- 使用 `agnes-image-2.0-flash` 模型
- 添加加载进度条和超时机制（120 秒）

### 测试方式
1. 说"你好"唤醒
2. 说"画一只可爱的猫咪"
3. 显示进度条，等待 30-60 秒
4. 图片显示在画布上

---

## PR #3: 风格转换功能

### 标题
`feat: 添加图片风格转换功能（油画、水彩、素描等）`

### 功能描述
支持将已生成的图片转换为不同艺术风格，包括油画、水彩、素描、卡通、动漫等。

### 实现思路
- 保存上次生成图片的提示词 `_lastImagePrompt`
- 风格转换时，组合原提示词 + 风格关键词
- 例如：原图"一只猫" + "油画风格" → "一只猫, oil painting style"
- 需要先生成图片才能进行风格转换

### 测试方式
1. 先说"画一只猫"生成图片
2. 说"油画风格"，图片转换为油画风格
3. 说"水彩风格"，图片转换为水彩风格

---

## PR #4: 局部重绘功能

### 标题
`feat: 添加局部重绘功能，支持修改图片特定部分`

### 功能描述
支持对已生成的图片进行局部修改，如"把耳朵改成蓝色"、"把背景换成沙滩"等。

### 实现思路
- 检测修改指令模式：`把...改成...`、`...换成...`
- 提取修改内容
- 组合原提示词 + 修改内容生成新图片
- 例如：原图"一只猫" + "蓝色耳朵" → "一只猫, 蓝色耳朵"

### 测试方式
1. 先说"画一只猫"生成图片
2. 说"把耳朵改成蓝色"
3. 生成蓝色耳朵的猫

---

## PR #5: 系统指令完善

### 标题
`feat: 添加完整系统指令（撤销、重做、清除、保存、帮助等）`

### 功能描述
实现完整的系统指令支持，包括撤销、重做、清空画布、保存图片、显示帮助、待机等。

### 实现思路
- 实现 `_handleSystemCommand` 方法
- 支持指令：撤销、重做、清除、保存、帮助、待机
- 支持模糊匹配和同义词识别
- 指令执行后重置唤醒定时器

### 测试方式
1. 说"你好"唤醒
2. 说"撤销"，执行撤销操作
3. 说"保存"，保存图片到下载文件夹
4. 说"帮助"，显示帮助信息

---

## PR #6: 多格式导出

### 标题
`feat: 支持多格式图片导出（PNG、JPG、WEBP）`

### 功能描述
支持将画布内容导出为多种图片格式，包括 PNG、JPG、WEBP。

### 实现思路
- 实现 `_saveCanvas(format)` 方法
- 使用 `canvas.toDataURL(mimeType)` 生成不同格式
- 自动生成带时间戳的文件名
- 通过创建 `<a>` 标签触发下载

### 测试方式
1. 生成一张图片
2. 说"保存"，保存为 PNG 格式
3. 说"保存为JPG"，保存为 JPG 格式

---

## PR #7: 画布管理功能

### 标题
`feat: 添加画布管理功能（新建、清空）`

### 功能描述
支持新建画布和清空画布操作，新建画布会清除当前内容。

### 实现思路
- 实现 `_newCanvas()` 方法
- 清空画布内容和历史记录
- 新建画布需要语音确认
- 使用 `_pendingConfirmation` 机制

### 测试方式
1. 说"新建画布"
2. 系统提示"确认要新建画布吗？"
3. 说"确认"，清空画布

---

## PR #8: 历史浏览功能

### 标题
`feat: 添加历史图片浏览功能（上一张、下一张）`

### 功能描述
支持浏览历史生成的图片，通过"上一张"、"下一张"指令切换。

### 实现思路
- 维护 `_canvasHistory` 数组保存历史状态
- 使用 `_historyIndex` 跟踪当前位置
- 每次生成图片自动保存到历史
- 使用 `canvas.toDataURL()` 和 `drawImage()` 切换

### 测试方式
1. 生成多张图片
2. 说"上一张"，显示上一张图片
3. 说"下一张"，显示下一张图片

---

## PR #9: 语音确认机制

### 标题
`feat: 添加语音确认机制，重要操作需确认`

### 功能描述
重要操作（如新建画布）需要语音确认，支持"确认"和"取消"指令。

### 实现思路
- 使用 `_pendingConfirmation` 存储待确认操作
- 说"确认"执行操作
- 说"取消"或"停止"取消操作
- 支持超时自动取消

### 测试方式
1. 说"新建画布"
2. 系统提示确认
3. 说"确认"执行，或说"取消"取消

---

## PR #10: UI 全面升级

### 标题
`feat: 全新 UI 设计，采用 Codex 浅色主题`

### 功能描述
全面升级界面设计，采用现代化浅色主题，优化布局和交互体验。

### 实现思路
- Codex 浅色主题：`#339cff` 主色调，白色背景
- 侧边栏布局：左侧工具栏 + 中间画布 + 右侧历史
- 浮动语音指示器：画布底部居中显示
- 字体优化：Outfit + Work Sans
- 响应式设计：适配不同屏幕尺寸

### 测试方式
1. 打开页面，查看新界面
2. 检查各元素显示正常
3. 测试响应式布局

---

## PR #11: 进度条加载动画

### 标题
`feat: 添加顶部进度条，优化加载体验`

### 功能描述
图片生成时显示顶部渐变进度条，替代原来的加载动画。

### 实现思路
- 实现 `#progress-bar` 元素
- 使用 CSS 动画实现渐变效果
- 通过 `.active` 类控制显示/隐藏
- 生成开始显示，生成完成隐藏

### 测试方式
1. 说"画一只猫"
2. 顶部显示渐变进度条
3. 生成完成后进度条消失

---

## PR #12: 文字通知系统

### 标题
`feat: 取消语音播报，改用文字通知`

### 功能描述
取消 TTS 语音播报，所有反馈改为文字提示框显示，避免回声问题。

### 实现思路
- 禁用 TTS 功能
- 所有 `speak()` 调用改为 `showToast()`
- 提示框居中显示在画布上方
- 支持成功、错误、信息等不同类型

### 测试方式
1. 执行任意操作
2. 屏幕中央显示文字提示
3. 无语音播报

---

## PR #13: 回声消除机制

### 标题
`feat: 实现回声消除，防止语音识别误触发`

### 功能描述
实现多层回声消除机制，防止麦克风拾取系统播报声音导致误识别。

### 实现思路
- TTS 播报期间停止语音识别
- 设置 `_blockRecognition` 标志位
- TTS 结束后 1 秒冷却期
- 只允许唤醒词"你好"打断播报

### 测试方式
1. 系统播报时说话
2. 不会触发指令
3. 说"你好"可以打断播报

---

## PR #14: 语音反馈控制

### 标题
`feat: 添加语音反馈控制指令（音量、语速、重复）`

### 功能描述
支持通过语音控制反馈系统，包括音量调节、语速调节、重复上次内容等。

### 实现思路
- 音量控制："大声点"、"小声点"
- 语速控制："说快点"、"说慢点"
- 重复功能："再说一遍"
- 静音控制："静音"

### 测试方式
1. 说"大声点"，音量增加
2. 说"说快点"，语速加快
3. 说"再说一遍"，重复上次内容

---

## PR #15: 绘图意图检测

### 标题
`feat: 实现绘图意图检测，区分绘图指令和普通对话`

### 功能描述
智能识别用户是否在下达绘图指令，避免将普通对话误识别为绘图请求。

### 实现思路
- 维护绘图关键词列表
- 检测动词：画、绘制、生成、创建等
- 检测对象：猫、狗、花、树、山、水等
- 检测风格：油画、水彩、素描等
- 非绘图指令循环询问

### 测试方式
1. 说"画一只猫"，触发绘图
2. 说"今天天气真好"，不触发绘图
3. 系统提示"请说出你想画的内容"

---

## PR #16: 循环询问机制

### 标题
`feat: 未识别指令时循环询问，引导用户正确操作`

### 功能描述
当用户指令未被识别时，系统会循环询问，引导用户说出正确的绘图指令。

### 实现思路
- 维护 `_clarifyCount` 计数器
- 第 1 次：提示"请说画一个什么"
- 第 2 次：提示"还是没听清"
- 第 3 次：提示"请描述你想画什么"
- 识别到有效指令后重置计数器

### 测试方式
1. 说"你好"唤醒
2. 说"今天天气真好"
3. 系统提示"请说画一个什么"
4. 说"画一只猫"，正常执行

---

## PR #17: 模糊匹配算法

### 标题
`feat: 实现模糊匹配，支持相似表达识别`

### 功能描述
支持相似表达的识别，如"画个圆"等同于"画一个圆"，"画只猫"等同于"画一只猫"。

### 实现思路
- 实现 `_match` 方法支持模糊匹配
- 移除填充词：一个、一只、请、帮我等
- 同义词映射：圆=圆形=圆圈，方=方形=矩形
- 归一化处理后匹配

### 测试方式
1. 说"画个圆"，识别为画圆
2. 说"画只猫"，识别为画猫
3. 说"帮我画个太阳"，识别为画太阳

---

## 使用说明

### 创建 PR 的步骤

1. **创建功能分支**
   ```bash
   git checkout -b feat/功能名称
   ```

2. **提交代码**
   ```bash
   git add .
   git commit -m "feat: 功能描述"
   ```

3. **推送分支**
   ```bash
   git push -u origin feat/功能名称
   ```

4. **创建 PR**
   - 标题：使用本文档中的标题
   - 描述：复制对应的功能描述、实现思路、测试方式

5. **合并 PR**
   - 确认代码审查通过
   - 合并到 main 分支

### PR 标题格式
```
feat: 功能描述
fix: 修复描述
docs: 文档描述
style: 样式描述
refactor: 重构描述
test: 测试描述
chore: 构建描述
```

---

*文档版本：v1.0*
*最后更新：2026-06-14*
