# 语音绘图工具 - 设计文档

## 1. 项目概述

本项目实现了一款纯语音控制的绘图工具，用户无需鼠标或键盘，仅通过语音指令即可在画布上进行绘图创作。应用基于 Web Speech API 实现语音识别，使用 HTML5 Canvas 进行绘图渲染，采用关键词匹配 + 模糊容错的自然语言理解策略解析用户指令。

**技术栈**: 原生 HTML/CSS/JavaScript + Node.js 静态服务器，零第三方依赖。

---

## 2. 用户故事规划与实现情况

### 2.1 计划支持的用户故事

| 编号 | 用户故事 | 优先级 | 实现状态 |
|------|---------|--------|---------|
| US-01 | 作为用户，我想通过语音说出形状名称（如"画一个圆"），让系统在画布上绘制该形状 | P0 | 已实现 |
| US-02 | 作为用户，我想通过语音指定颜色（如"用红色画一个圆"），让系统用该颜色绘制 | P0 | 已实现 |
| US-03 | 作为用户，我想通过语音指定大小（如"画一个大圆"），让系统按指定大小绘制 | P0 | 已实现 |
| US-04 | 作为用户，我想通过语音指定位置（如"在中间画一个圆"），让系统在指定位置绘制 | P0 | 已实现 |
| US-05 | 作为用户，我想通过语音清除画布（"清除"/"清空"），重新开始创作 | P0 | 已实现 |
| US-06 | 作为用户，我想撤销上一步操作（"撤销"），回退到之前的状态 | P0 | 已实现 |
| US-07 | 作为用户，我想重做被撤销的操作（"重做"） | P1 | 已实现 |
| US-08 | 作为用户，我想用一句话下达复合指令（如"用红色画一个大圆在中间"） | P0 | 已实现 |
| US-09 | 作为用户，我想连续下达多个指令（如"画一个圆然后画一个方块"） | P1 | 已实现 |
| US-10 | 作为用户，我想只说颜色名（如"红色"）来切换当前颜色，无需重复说"画" | P1 | 已实现 |
| US-11 | 作为用户，我想通过"大一点"/"小一点"调整当前绘图大小 | P1 | 已实现 |
| US-12 | 作为用户，我想看到当前的颜色、大小、工具设置状态 | P0 | 已实现 |
| US-13 | 作为用户，我想看到命令执行历史记录 | P1 | 已实现 |
| US-14 | 作为用户，我想在操作后听到语音反馈确认 | P1 | 已实现 |
| US-15 | 作为用户，我想绘制特殊图形（波浪线、虚线、螺旋、五角星、爱心、箭头等） | P2 | 已实现 |
| US-16 | 作为用户，我想通过语音写文字（如"写文字你好"） | P2 | 已实现 |
| US-17 | 作为用户，我想暂停/恢复语音识别（麦克风开关） | P1 | 已实现 |
| US-18 | 作为用户，我想查看语音指令参考面板 | P2 | 已实现 |
| US-19 | 作为用户，我想通过语音自由绘制路径（如"从左到右画一条线"） | P2 | 部分实现 |
| US-20 | 作为用户，我想通过语音进行连续自由绘图（类似画笔模式） | P3 | 未实现 |

### 2.2 未完成部分说明

**US-19（部分实现）**: 当前支持画直线、水平线、竖线，以及预设路径的波浪线/虚线/螺旋。但不支持更精细的路径描述（如"从左上角画到右下角"的坐标级控制），因为自然语言中的空间坐标描述歧义较大，需要更复杂的 NLU 模型支持。

**US-20（未实现）**: 纯语音的连续自由绘图需要一种"开始画画"/"停止画画"的模式切换，以及路径的实时控制。这在纯语音交互下体验较差——用户无法实时感知笔触位置，容易画出意料之外的轨迹。设计上认为此功能在纯语音场景下实用价值有限，故搁置。

---

## 3. 指令能力一览

### 3.1 已实现的绘图形状（13种）

圆形、方形/矩形、三角形、直线、五角星、箭头、爱心、菱形、五边形、六边形、波浪线、虚线、螺旋

### 3.2 已实现的颜色（13种）

红、蓝、绿、黄、紫、橙、黑、白、粉、青、棕、灰 + 英文名称支持

### 3.3 已实现的大小（4级）

小（30px）、中等（60px）、大（100px）、超大（150px）+ 相对调整（大一点/小一点）

### 3.4 已实现的位置（9个）

左上、上、右上、左、中间、右、左下、下、右下

### 3.5 已实现的操作命令

清除、撤销、重做、颜色切换、形状切换、大小调整

### 3.6 支持的表达方式

- 标准指令: "画一个红色的大圆在中间"
- 简略指令: "红色"（切换颜色）、"大一点"（调整大小）
- 复合指令: "画一个圆然后画一个方块"（按"然后/接着/再/并且"拆分）
- 多种同义词: "画"、"绘制"、"画出"、"画上"等均识别为绘图动作

---

## 4. 容错与准确性策略

### 4.1 语音识别层

- **持续监听 + 中间结果展示**: 使用 `continuous: true` + `interimResults: true`，实时展示识别中的文本，让用户感知系统正在"听"
- **自动重启**: 识别结束后自动重启监听，避免因静默中断而停止工作
- **多候选支持**: 设置 `maxAlternatives: 3`，虽然当前只取第一候选，但预留了后续扩展空间

### 4.2 语义理解层

- **关键词优先级匹配**: 长关键词优先于短关键词（如"五角星"优先于"星"），避免部分匹配导致误判
- **宽松匹配策略**: 使用 `includes()` 而非精确匹配，容忍语音识别中的冗余词（如"帮我画一个圆"中的"帮我"）
- **多模式覆盖**: 支持"画一个X"、"个X"、"X"等多种表达模式
- **独立颜色/大小命令**: 用户只说"红色"或"大一点"也能正确执行，无需每次都说完整的"画一个X"
- **默认值兜底**: 复合指令中缺少某个维度时，使用当前设置的默认值

### 4.3 反馈层

- **视觉反馈**: Toast 通知显示执行结果（成功/失败）、命令历史面板记录所有指令
- **语音反馈**: TTS 语音播报执行结果，让纯语音交互更加自然
- **状态面板**: 顶部显示当前颜色、大小、工具设置，用户随时了解系统状态

---

## 5. 响应延迟优化

### 5.1 设计决策

- **Web Speech API（浏览器原生）**: 使用浏览器内置的语音识别引擎，无需网络请求到外部 STT 服务，识别延迟通常在 200-500ms
- **本地命令解析**: 命令解析使用纯 JavaScript 关键词匹配，执行时间 < 1ms
- **Canvas 即时渲染**: 所有形状使用 Canvas 2D API 原生绘制，无需图片加载或预处理
- **零网络依赖（核心流程）**: 语音识别 → 命令解析 → 画布绘制 全链路在本地完成，无需任何网络请求

### 5.2 实测表现

- 从用户说完话到形状出现: 约 300-800ms（主要耗时在语音识别的 final result 产出）
- 命令解析 + 画布绘制: < 5ms
- TTS 语音反馈: 约 200ms 开始播放

---

## 6. 复杂指令拆解与执行

### 6.1 复合指令拆解

系统支持使用连接词拆分多步指令：
- 触发词: "然后"、"接着"、"再"、"并且"
- 拆分后每步独立解析、顺序执行
- 每步之间间隔 400ms，配合动画效果

示例: "画一个红色的圆然后画一个蓝色的方块在右边" →
1. 画红色圆形（默认位置）
2. 400ms 后画蓝色方形（右边）

### 6.2 隐式上下文继承

- 未指定颜色时使用当前颜色
- 未指定大小时使用当前大小
- 未指定位置时在上次绘制位置附近随机偏移放置

---

## 7. 运营成本控制策略

### 7.1 想到的策略

| 策略 | 说明 | 是否采用 |
|------|------|---------|
| 使用浏览器原生 STT | 避免调用付费云端 ASR 服务 | 采用 |
| 无后端 AI 推理 | 命令解析使用规则引擎而非 LLM | 采用 |
| 纯前端架构 | 静态文件服务器，无数据库、无计算后端 | 采用 |
| 图像压缩与帧采样 | 如使用云端视觉 API，可降低图片分辨率和采样率 | 未使用（无云端视觉需求） |
| 音频 VAD 静默检测 | 过滤静音段，减少无效识别请求 | 部分采用（Web Speech API 内置 VAD） |
| 会话超时自动关闭 | 空闲 N 分钟后自动停止监听 | 未采用（可后续添加） |
| 响应长度限制 | 限制 TTS 反馈文本长度，减少合成开销 | 采用（反馈文本简短） |
| 本地存储历史 | 使用内存存储，不持久化到后端 | 采用 |
| CDN 托管静态资源 | 生产部署时使用 CDN 减少服务器带宽 | 未部署（本地开发阶段） |
| 形状缓存池 | 预渲染常用形状到离屏 Canvas | 未采用（当前性能已足够） |

### 7.2 实际成本分析

本应用的核心交互链路（语音识别 → 指令解析 → 画布绘制 → TTS 反馈）完全在浏览器本地完成，运营成本趋近于零：

- **语音识别**: 使用浏览器原生 Web Speech API，由浏览器厂商（Chrome 使用 Google 的语音识别服务）提供，对开发者免费
- **指令解析**: 纯前端 JavaScript 规则引擎，零服务端成本
- **画布绘制**: Canvas 2D 原生 API，零成本
- **语音反馈**: 使用浏览器原生 SpeechSynthesis API，免费
- **服务器**: 仅需一个静态文件服务器（Node.js 原生 HTTP 模块），带宽和计算开销极低

**唯一的服务端成本**: 静态文件托管（可通过 GitHub Pages、Vercel 等免费方案解决）。

---

## 8. 架构设计

```
┌─────────────────────────────────────────────────────┐
│                    浏览器前端                         │
│  ┌──────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │ SpeechManager │  │CommandParser│  │ DrawingEngine│ │
│  │ (Web Speech   │  │ (关键词匹配  │  │ (Canvas 2D  │ │
│  │  API)         │  │  + 模糊容错) │  │  渲染引擎)   │ │
│  └──────┬───────┘  └──────┬──────┘  └──────┬─────┘ │
│         │                  │                │       │
│         └──────────┬───────┘                │       │
│                    │                        │       │
│              ┌─────▼──────┐                 │       │
│              │    App      │─────────────────┘       │
│              │ (主控制器)   │                         │
│              └─────┬──────┘                         │
│                    │                                │
│              ┌─────▼──────┐                         │
│              │  UIManager  │                         │
│              │ (UI 更新)   │                         │
│              └────────────┘                         │
└─────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────┐
│  Node.js 静态    │  ← 仅提供 HTML/CSS/JS 文件托管
│  文件服务器       │
└─────────────────┘
```

---

## 9. 文件结构

```
ai_viocetool/
├── server.js          # Node.js HTTP 静态文件服务器
├── package.json       # 项目元数据（零依赖）
├── serve.ps1          # PowerShell 备用服务器
├── DESIGN.md          # 本文档
└── public/
    ├── index.html     # 主页面（UI 结构）
    ├── style.css      # 样式（暗色主题 + 动画）
    └── app.js         # 全部应用逻辑
        ├── SpeechManager   # 语音识别管理
        ├── CommandParser   # 中文 NLU 命令解析
        ├── DrawingEngine   # Canvas 绘图引擎（支持撤销/重做）
        ├── UIManager       # UI 状态管理
        └── App             # 主应用控制器
```

---

## 10. 使用说明

1. 使用 Chrome 浏览器打开 `http://localhost:3001`
2. 点击"点击开始语音绘图"按钮（首次需要授权麦克风权限）
3. 说出绘图指令，例如：
   - "画一个红色的大圆在中间"
   - "用蓝色画一个五角星"
   - "画一个爱心在左上角"
   - "撤销"
   - "清除"
4. 顶部设置面板显示当前颜色、大小和工具
5. 左侧面板显示命令执行历史
6. 点击 ? 按钮查看完整指令参考

---

## 11. 已知限制

1. **浏览器兼容性**: Web Speech API 仅在 Chrome/Edge 中完整支持，Firefox/Safari 支持有限
2. **需要 HTTPS 或 localhost**: Web Speech API 的安全策略要求安全上下文
3. **中文语音识别质量**: 依赖浏览器内置引擎，在嘈杂环境或方言口音下识别率可能下降
4. **首次需要点击**: 浏览器安全策略要求用户手势才能启动语音识别
5. **无持久化**: 画布内容和命令历史在页面刷新后丢失
